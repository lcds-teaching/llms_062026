# Large Language Models for Health and Social Science Research
#
# This file is intentionally conservative. The setup guide recommends Anaconda,
# which already includes many data-science packages. These are the Python
# packages needed for the current notebooks and common course extensions.
#
# Install inside the course environment with:
#   python -m pip install -r requirements.txt

jupyter>=1.1
ipykernel>=6.29

numpy>=1.26
pandas>=2.2
scikit-learn>=1.4

